{
  "name": "bundled-compression-runtime",
  "private": true,
  "type": "module",
  "description": "Bundled compression runtime for comic",
  "scripts": {
    "compress": "npx -y bun scripts/main.ts",
    "test": "npm --prefix scripts test"
  }
}
